# 小红书舆情监控助手：安装与使用说明

## 1. 功能概览

- 从当前已登录的小红书页面读取可见 DOM，不读取 Cookie，不调用隐藏接口。
- 拉取帖子正文、图片和当前可见评论，写入本地 Excel 与 SQLite。
- 可配置 AI 对帖子和评论进行情绪分类，显示进度与完成通知。
- 侧边栏可缩小为浏览器右侧悬浮球。

## 2. 首次安装

### 方式 A：一键向导（推荐）

1. 安装 Python 3.10 或更高版本。
2. 在项目根目录安装依赖：

   ```powershell
   pip install -r bridge/requirements.txt
   ```

3. 打开 `chrome://extensions`，开启“开发者模式”，点击“加载已解压的扩展程序”，选择本项目的 `extension` 文件夹。
4. 双击根目录的 `setup.cmd`。向导会尝试自动识别插件 ID；未识别时，复制扩展卡片上的 32 位 ID 并粘贴到窗口。
5. 返回 `chrome://extensions`，点击扩展卡片上的“重新加载”，然后刷新小红书页面。

### 方式 B：手动安装

```powershell
powershell -ExecutionPolicy Bypass -File bridge/build_native_host.ps1
powershell -ExecutionPolicy Bypass -File bridge/install_native_host.ps1 -ExtensionId "你的32位插件ID"
```

指定现有笔记 CSV 时，可追加：

```powershell
-SeedCsv "D:\路径\小红书_笔记总表.csv"
```

旧版 XLSX 可继续通过 `-SeedXlsx` 传入一次；Bridge 校验后会自动迁移成笔记、评论两个 UTF-8 CSV。

## 3. 配置监控词库

首次运行向导会创建 `bridge/data/relevance_keywords.json`。它是本机私有配置，不会被 Git 提交：

```json
{
  "brand": ["品牌名", "品牌别名"],
  "products": ["产品名 A"],
  "accounts": ["官方账号名"]
}
```

修改后重启 Bridge，或在任务管理器结束 `xhs_monitor_native_host.exe` 后重新打开插件侧边栏。

## 4. 日常操作

1. 登录小红书并打开搜索结果、首页或帖子详情。
2. 点击扩展图标打开侧边栏。
3. 点击“核对当前页面”识别帖子；点击卡片上的“拉取”读取详情并写入 CSV。
4. 首次启动且没有总表时，程序会自动创建独立的 `*_笔记总表.csv` 和 `*_评论总表.csv`（UTF-8 BOM）。
5. 素材图片保存在总表旁的 `posts_materials` 文件夹。
6. 在设置中填写 AI 服务参数，点击“测试连接”；成功后会显示提示。分析结果写回对应 CSV。
7. 点击侧边栏右上角缩小按钮可切换为右侧悬浮球；点击小球重新展开。

## 5. 数据与隐私

- `data/`：笔记/评论 CSV、周报和素材等用户数据；默认不提交 Git。
- `bridge/data/`：SQLite、AI 配置、私有词库；默认不提交 Git。
- `bridge/exports/`：运行导出与调试数据；默认不提交 Git。
- Bridge 只监听 `127.0.0.1:17881`。
- 扩展权限中没有 `cookies` 和 `webRequest`。

## 6. 常见问题

### 插件显示 Bridge 离线

重新双击 `setup.cmd`，确认插件 ID 与 `chrome://extensions` 中一致；再点击“重新加载”。

### 修改词库后没有生效

结束 `xhs_monitor_native_host.exe` 进程并重新打开侧边栏。

### AI 测试失败

检查 API Key、接口地址、模型名和网络；Bridge 本身可通过浏览器访问 `http://127.0.0.1:17881/api/health` 检查。

### 卸载本地连接

```powershell
powershell -ExecutionPolicy Bypass -File bridge/uninstall_native_host.ps1
```
