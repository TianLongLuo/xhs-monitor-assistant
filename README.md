# 小红书舆情监控助手

一个面向品牌舆情监控的 Chrome Manifest V3 扩展与 Windows 本地 Bridge。扩展读取当前小红书页面已加载的 DOM，采集帖子、评论、图片和视频，并幂等写入本地 UTF-8 CSV 与 SQLite。

公开版版本：`0.34.14`

## 公开版说明

- 仓库不包含任何具体品牌名称、产品词、账号词、本机目录、Cookie、API Key 或业务数据。
- 首次安装后，请在本机配置自己的品牌词库；私有词库与运行数据均被 `.gitignore` 排除。
- 所有采集结果默认只保存在本机，Bridge 仅监听 `127.0.0.1:17881`。
- 扩展不申请 Cookie、`webRequest` 等权限，也不调用小红书隐藏接口。

## 主要功能

- 当前页面帖子核对与增量扫描。
- 帖子正文、一级/二级评论、图片与视频素材采集。
- CSV / SQLite 幂等写入、严格按笔记 ID 去重。
- 已拉取帖子评论增量同步与全库巡检。
- 数据体检、同步变更中心、重点帖子观察名单与近 7 天周报。
- 可选 DeepSeek：品牌相关性判断、正文与评论总结、评论回复建议。
- 侧边栏与详情页磁吸 Process 面板。

## 环境要求

- Windows 10/11
- Chrome 114+
- Python 3.10+

## 直接 Clone 与安装

```powershell
git clone https://github.com/TianLongLuo/xhs-monitor-assistant.git
cd xhs-monitor-assistant
pip install -r bridge/requirements.txt
```

1. 打开 `chrome://extensions` 并启用“开发者模式”。
2. 点击“加载已解压的扩展程序”，选择仓库中的 `extension` 目录。
3. 双击 `setup.cmd`，按向导完成 Native Messaging 注册。
4. 回到扩展管理页点击“重新加载”，然后刷新已打开的小红书页面。

详细安装与故障排查见 [使用说明.md](使用说明.md)。

## 配置品牌词库

安装向导会根据示例在本机生成：

```text
bridge/data/relevance_keywords.json
```

示例格式：

```json
{
  "brand": ["品牌名", "品牌别名"],
  "products": ["产品名 A", "产品名 B"],
  "accounts": ["官方账号名"]
}
```

也可以编辑 `extension/relevance.js` 中的公开占位词，或通过本地 Bridge 维护私有词库。

## 目录结构

```text
extension/                         Chrome 扩展
bridge/
  server.py                       本地 HTTP Bridge
  native_host.py                  Native Messaging 入口
  build_native_host.ps1           构建 Native Host
  install_native_host.ps1         注册扩展 ID
  uninstall_native_host.ps1       移除注册
  relevance_keywords.example.json 词库示例
data/                              运行时生成，不提交
setup.ps1 / setup.cmd              一键安装向导
使用说明.md
```

## 手动安装 Native Host

```powershell
powershell -ExecutionPolicy Bypass -File bridge/build_native_host.ps1
powershell -ExecutionPolicy Bypass -File bridge/install_native_host.ps1 -ExtensionId "你的32位插件ID"
```

如需导入已有笔记 CSV：

```powershell
powershell -ExecutionPolicy Bypass -File bridge/install_native_host.ps1 \
  -ExtensionId "你的32位插件ID" \
  -SeedCsv "D:\path\to\小红书_笔记总表.csv"
```

评论 CSV 会在同目录自动创建。旧版 `-SeedXlsx` 可用于一次性迁移。

## 本地数据与隐私

以下内容默认不进入 Git：

- `bridge/data/`
- SQLite 数据库
- 笔记与评论 CSV
- 素材目录
- AI 设置与加密后的 API Key
- 本机 Native Messaging 清单
- 私有品牌词库

## 常用本地 API

```text
GET  /api/health
GET  /api/stats
GET  /api/notes
GET  /api/comments
GET  /api/relevance
POST /api/scan
POST /api/pull
POST /api/comments/upsert
POST /api/ai/test
POST /api/ai/analyze
POST /api/data-health/repair-relations
```

## 卸载 Native Host

```powershell
powershell -ExecutionPolicy Bypass -File bridge/uninstall_native_host.ps1
```

## v0.25.2：语义分析字段与评论存续状态

- 接纳笔记/评论 CSV 的 `语义分析次数`、`分析结论是否差评`、`差评类型`、`差评子类型`，并与 SQLite 保持一致。
- 评论 CSV 新增 `评论状态`，值固定为 `存在` 或 `已删除`；历史评论初始化为 `存在`。
- 完整评论同步不再物理删除消失评论，而是保留正文和语义字段并标记 `已删除`；重新出现时恢复为 `存在`。
- 只有评论已完整展开的 `likely_complete` 快照才允许标记删除，部分加载不会误判。
- SQLite 与素材 `comments.json` 同步保存评论状态和核验时间，数据体检核对 ID、状态和语义字段。

## v0.25.3：帖子存续状态与非破坏性下架处理

- 笔记 CSV 新增 `帖子状态`，固定为 `存在` 或 `已删除`；SQLite 同步保存状态、删除时间与最近核验时间。
- 双重证据确认帖子删除或下架时，只更新状态，不删除帖子行、评论、分析记录或素材目录。
- 新帖子继续按笔记 ID 追加；已删除帖子重新成功打开或拉取后恢复为 `存在`。
- 批量同步默认跳过已标记删除的帖子，人工选择时仍可重新核验。
- 数据体检同时核对笔记 CSV、SQLite 与素材 `note.json` 的帖子状态。

## v0.25.4：同步状态全存储一致性

- 每次单帖拉取、评论核对和批量同步都会同步帖子与评论的 `存在/已删除` 状态，并在成功前校验 CSV、SQLite、`note.json`、`comments.json` 与正文快照。
- 只有已展开全部回复、滚动到底并稳定至少两轮的可信完整评论快照，才会把消失评论软标记为 `已删除`；零评论还必须识别到页面明确空态。部分或失败快照继续保留历史状态。
- 同步前创建 CSV、SQLite 与受管素材持久检查点；任一下载、字段或状态校验失败时整体回滚，包括新增或改写的图片、视频和快照。进程异常退出后会在下次启动恢复；若检测到外部 CSV 修改则保留冲突副本并停止覆盖。
- 评论 CSV 新增 `评论用户主页url`，并修复话题逐字符展开、中文评论层级、父评论保留及跨帖子评论 ID 冲突。
- 数据体检新增帖子/评论字段漂移、素材快照字段校验及评论 ID 别名/逻辑重复候选；候选只提示审计，不会按文本自动合并。安全修复会规范话题、层级、关联链接、素材索引并重新生成快照。

## v0.25.5：磁吸窗口稳定与批量同步降抖

- 磁吸 Process 固定复用同一详情容器，使用位置滞回与 3 像素容差避免平台 DOM 细微重排造成左右跳动。
- Process 自身字段、评论和进度更新不再触发外层定位观察；仅小红书详情结构变化才重新核对锚点。
- 批量同步失败列表只在内容真正变化时重绘，进度文本采用幂等更新，减少侧边栏闪动和滚动位置漂移。

### 数据总览 0.34.9：行状态与筛选导出

- 已删除记录淡粉底，已发布的语义差评淡绿底，同时命中时粉底＋绿边。所属帖已删除的评论也标粉；旧 AI 负面标记、访问待复核不等同于这两种状态。
- 默认 Excel 成组导出：左侧一级评论上下文合并，右侧逐条筛选结果；附 high/medium 子表、统计摘要、分帖汇总和全部原始字段。只导出当前筛选命中的记录，不受已加载页数或勾选行数影响，也不擅自额外限定为未删除差评。
- 一级评论未命中筛选时仍可作为关联上下文出现，不额外增加筛选条数；父链缺失、循环、跨帖时明确标记待核验。
- Main Category / Subcategory 原样采用已发布的差评类型、子类型；无翻译、无明确专项 high/medium 或判断依据时留空，旧 risk_level 不当作专项控制级别。
- CSV 仍可选，导出全部筛选记录的当前可见数据字段。Excel 导出全部 API 数据字段，原始数据表保留查询顺序和精确 ID。
- 导出时冻结筛选条件并核对快照、页数和记录数，校验失败不生成部分文件。Excel 单元格超长或文件超过浏览器传输上限时明确报错，可选 CSV。
- Excel 功能需要 0.34.9 Bridge；升级后重新加载扩展并重新打开数据总览。


### 数据总览 0.34.10：已有值多选

- 列筛选与筛选规则中的“等于 / 属于多项”可勾选多个已有值，自动使用“属于多项”，同一字段任一值命中即可；其他字段继续遵循原有“全部 / 任一规则”逻辑。
- 支持搜索选项、查看已选数量、标签移除和重新打开回显；搜索不清空已选值。每条规则最多 100 项。
- 多选值以数组保存、查询和导出，包含逗号的分类不会被拆成两项；手动输入仍支持逗号分隔，含逗号的完整值使用 JSON 数组显示。
- 范围、大小比较、包含等条件保留其原有单值含义；空值使用“为空”规则。更新后重新加载扩展并重新打开数据总览即可，Bridge 无需重启。


### 数据总览 0.34.11：定位原帖后返回评论位置

- 从评论“定位原帖”进入帖子数据库后，筛选工具栏显示“← 返回评论位置”。
- 点击返回恢复离开时的搜索、筛选、排序、语义检索结果、已加载的全部评论页、选择及横纵滚动位置；原评论短暂描边高亮，不重新查询首批记录。
- 返回点仅保存在当前页面内存中；刷新/关闭页面或手动切换回评论数据库后结束这次往返。迟到的帖子查询不会覆盖返回后的评论。
- 若离开后数据快照发生变化，仍可返回查看原位置，但锁定导出和删除并提示更新数据。无需升级或重启 Bridge。


### 数据总览 0.34.12：IP 属地包含未显示

- 评论 IP 属地、帖子 IP 属地及评论关联的原帖 IP 属地，已有值多选列表增加“未显示（空值）”，可单独选择，也可与多个地区同时选择。
- 缺失项匹配 NULL、空字符串及空格，不把“未显示”伪造为数据库地区值；同字段地区与缺失项按 OR 匹配，其他字段仍遵循全局规则关系。
- 空值选择随筛选保存、回显和导出，搜索地区不会取消空值选择。列表未额外请求或估算缺失记录数量。
- 仅需重新加载扩展并重新打开数据总览，无需重启 Bridge。


### 数据总览 0.34.13：真正冻结列与紧凑操作栏

- 固定列立即移入左侧冻结区，包含末尾“操作”列；同步表头、数据行、合并单元格与列宽位置。取消固定恢复用户原顺序，不重查数据，不改变 CSV 字段顺序。
- 默认仅显示搜索、筛选、字段、排序、重置、计数、删除、导出和刷新；隐藏大标题与成功校验横幅。校验失败、快照变化和未完成筛选的警告仍保留。
- 语义检索、差评/强硬销售/价格差异/过敏快捷词与说明收进搜索框左侧菜单；搜索按钮和 Enter 保留原先明确提交的语义，未提交时不擅自执行检索。行颜色说明移到字段设置。
- 同步进度标签相同文字不重复写 DOM，仍逐次查询当前面板节点；五秒心跳、采集回调、分页等待、完整性检查与回滚均不变。这是 UI 开销优化，不宣称总体同步耗时下降比例。
- 性能审计发现批末逐帖一致性校验会重复解析两份 CSV：N 帖的读取阶段存在 2N 次解析。后续可评估锁内“写后磁盘只读索引”复用，理论减少为两次，但需验证外部文件变化与回滚路径后再实施；本版未改这一校验路径。
- 重新加载扩展即可生效；同步进度优化还需刷新已打开的小红书页面。Bridge 不需要重启。


### 同步优化 0.34.14：批内复用与有界预读

- 批量访问状态写入及素材刷新全部完成后，才从磁盘读取两张 CSV，建立仅限本批次、当前线程的校验索引。逐帖 SQLite、素材文件、字段值、重复 ID、跨帖 ID、删除状态与哈希校验照常执行。单帖接口不使用批缓存。
- CSV 解析次数由每批 `2N` 次降为 `2` 次，另外保留批末两次流式摘要复读。每次使用前检查路径/文件身份、纳秒时间与大小，退出时重验内容摘要；发现外部改动或读失败即失败，进入原有回滚/冲突保护流程，不回退到旧缓存。
- 提交阶段复用紧邻产生的标准化评论，移除一次重复标准化；公开预览仍自行验证和标准化，提交仍在锁内读取当前数据库重新比较，不采用旧预览结论或客户端“已验证”标志。
- 同一个后台标签页读取下一帖时，可并行完成当前帖的本地提交。最多一个下一帖快照，快照完成读取后深拷贝；读取彼此串行、提交彼此串行。保留 420ms 翻帖间隔及全部采集等待、重试、完整性证据。取消会等待正在提交的写入收尾，丢弃尚未提交的预读，读任务收尾后才关闭标签页。
- 迟到的上一帖进度消息由后台及页面双重检查，不重建上一帖面板。批次绑定开始时的 Bridge 地址，配置变更后不向新的地址提交这批数据。
- 无变化的帖子仍执行写入与全存储一致性校验。部分读取仍保留历史评论，不提升为完整状态；原有失败记录、重试、提醒偏好、状态批写及周报流程保留。
- 以上是减少重复工作和重叠等待，不承诺固定的整批提速比例。实际耗时仍受页面加载、评论量、素材与磁盘状态影响。应用新后端需更新并重启 Bridge；已有同步进行中时不要重启。扩展重新加载后刷新小红书页面。
