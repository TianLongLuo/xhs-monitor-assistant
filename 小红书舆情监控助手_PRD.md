# 小红书舆情监控助手：当前状态与产品需求文档

文档日期：2026-08-11
当前版本：0.18.0
部署形态：Chrome Manifest V3 扩展 + Windows Native Messaging Host + 本地 HTTP Bridge

## 1. 当前本地运行状态

### 1.1 总体结论

当前插件与 Bridge 正常运行，运行路径、Chrome 加载路径、Native Messaging 注册表及主要数据路径均已切换到：

`C:\Users\Kim81\Documents\workspace\Marketing\舆论\舆论监控插件联动`

### 1.2 运行检查结果

| 项目 | 当前状态 |
|---|---|
| Bridge 健康检查 | 正常，`ok=true` |
| Bridge 版本 | `0.24.1` |
| 本地服务 | `http://127.0.0.1:17881` |
| Native Host | `com.xhsmonitor.bridge` |
| Native Host 程序 | `舆论监控插件联动\bridge\dist\xhs_monitor_native_host.exe` |
| Chrome 插件 ID | `kbmhklcbiobifehkcpknngmichcogeho` |
| Chrome 当前加载目录 | `舆论监控插件联动\extension` |
| SQLite | `舆论监控插件联动\bridge\data\xhs_monitor.db` |
| 笔记 CSV | `舆论监控插件联动\data\Origani_total_post\Origani_笔记总表.csv` |
| 评论 CSV | `舆论监控插件联动\data\Origani_total_post\Origani_评论总表.csv` |
| 素材目录 | CSV 同目录下的 `posts_materials` |

系统中可看到两个同名 Native Host 进程，它们是 PyInstaller one-file 程序的父子进程关系，不代表启动了两个独立 Bridge 服务。

### 1.3 当前数据库数据

| 指标 | 数量 |
|---|---:|
| 总记录 | 298 |
| 相关记录 | 242 |
| Excel 已有 | 98 |
| 新相关记录 | 144 |
| 忽略记录 | 56 |
| Excel 缺失 | 144 |

当前活动数据库中未发现指向旧目录 `舆论监控2026.7.2` 的路径。

### 1.4 当前 AI 状态

| 项目 | 状态 |
|---|---|
| AI 已配置 | 是 |
| 接口 | DeepSeek 官方 API |
| 模型 | `deepseek-v4-flash` |
| 帖子自动分析 | 已开启 |
| 评论自动分析 | 已开启 |
| 最大并发 | 1 |
| 每日调用上限 | 300 |
| 当日已使用 | 17 |
| 已完成任务 | 19 |
| 历史失败任务 | 14 |
| 当前评论采集任务 | 0 |

历史失败任务主要为模型返回空内容，发生时间集中在 2026-08-05；当前没有排队中或执行中的 AI 任务。

### 1.5 本地数据规模

- 用户数据约 2.1 GB。
- 文件约 1,059 个。
- 笔记与评论总表使用两个独立的 UTF-8 BOM CSV。
- 素材目录存在。

## 2. 产品目标

为品牌舆情人员提供一个在本机运行的小红书信息采集与分析工具。用户在正常登录和浏览小红书的过程中，从当前页面可见 DOM 读取帖子、评论与图片链接，将结果保存到本地 CSV / SQLite，并可调用 AI 完成情绪判断、风险分类和结果回写。

## 3. 目标用户

1. 品牌市场与舆情人员。
2. 客服、消费者洞察与门店运营人员。
3. 需要将浏览器页面信息结构化保存到 CSV 的个人用户。

## 4. 核心使用流程

1. 用户安装 Python 依赖并在 Chrome 中加载 `extension`。
2. 用户运行 `setup.cmd`，向导自动识别或要求输入插件 ID。
3. 向导构建 Native Host、生成本地配置、注册 Native Messaging 并执行健康检查。
4. 用户登录小红书，打开搜索结果、推荐页或帖子详情。
5. 用户打开插件侧边栏并执行页面核对。
6. 用户点击帖子上的“拉取”，扩展读取当前页面详情、评论和图片 URL。
7. Bridge 将帖子、评论和素材写入 SQLite、两个 CSV 及素材目录。
8. AI 自动分析任务入队，界面显示状态、估算百分比与完成提示。
9. 用户在 WPS/CSV 或插件运营中心查看和复核结果。

## 5. 功能需求

### FR-01 页面识别

- 支持识别当前小红书页面中已加载的帖子卡片。
- 支持搜索页、首页、推荐流及帖子详情。
- 支持继续下拉后对新增 DOM 卡片进行识别。

### FR-02 相关性判断

- 从本机私有文件 `bridge/data/relevance_keywords.json` 读取品牌词、产品词和账号词。
- Bridge 通过 `/api/relevance` 将词库下发给扩展。
- 私有词库不得进入公开 Git 仓库。

### FR-03 帖子与评论拉取

- 读取当前页面可见 DOM，不读取浏览器 Cookie。
- 拉取标题、正文、作者、话题、互动量、发布时间、图片及可见评论。
- 支持主评论、子评论和评论稳定 ID。
- 不宣称评论完整；无法证明完整时应显示“部分采集”。

### FR-04 本地写入

- 按帖子 ID、标题和正文进行幂等匹配。
- 写入本地 SQLite、笔记 CSV 与评论 CSV。
- 首次运行没有总表时自动创建两个 UTF-8 BOM CSV。
- 图片保存到 CSV 同目录的 `posts_materials`。

### FR-05 AI 分析

- 支持 AI 连接测试。
- 帖子和评论拉取后自动进入分析队列。
- 分析结果包括情绪、风险等级、问题分类、摘要、置信度和建议动作。
- 分析成功后写回对应 CSV 并显示底部居中完成提示。
- 失败任务保留错误原因并支持重试。

### FR-06 进度反馈

- Process 面板显示帖子拉取步骤。
- AI 队列显示排队、分析、完成和失败状态。
- 显示流式视觉进度条与百分比。
- 当前百分比是基于任务状态和耗时的估算值，不是模型逐 Token 的真实流式进度。

### FR-07 界面形态

- 支持 Chrome 侧边栏。
- 支持缩小为浏览器右侧圆形悬浮球。
- 支持悬浮球上下拖动、位置记忆和点击展开。
- AI 运行时悬浮球显示脉冲状态。

### FR-08 安装与连接

- 根目录提供 `setup.cmd` 和 `setup.ps1`。
- 自动或手动获取 32 位 Chrome 插件 ID。
- 自动构建、注册 Native Host 并验证 `/api/health`。
- 插件 ID 变化后可重新运行向导或 `重新注册插件ID.ps1`。

## 6. 非功能需求

### 安全与隐私

- Bridge 仅监听 `127.0.0.1`。
- 扩展不申请 `cookies`、`webRequest` 权限。
- AI Key、SQLite、Excel、素材、私有词库和本机注册清单由 `.gitignore` 排除。
- 公开仓库不包含个人绝对路径、私有品牌词或访问 Token。

### 稳定性

- Excel 和 SQLite 写入必须幂等。
- Bridge 离线时不得伪装写入成功。
- 旧词库缺失或修改时，启动过程不得破坏已有记录状态。

### 可迁移性

- 所有程序文件位于同一项目根目录。
- 新用户无需准备 Excel。
- 公开代码仓库可直接克隆，运行时数据由用户本地生成。

## 7. 已知限制

1. 插件只读取页面当前可见和实际加载的 DOM，无法保证获得平台全部评论。
2. 图片下载依赖页面提供的 CDN URL。
3. Chrome 普通扩展窗口不支持真正透明，悬浮球外围可能存在小型窗口背景。
4. AI 百分比为估算进度。
5. 当前有 14 个历史 AI 失败任务，需用户按需重试或清理。
6. 新目录中的部分旧交接文档和独立 Python 脚本仍以文本形式记录旧目录；它们不参与当前插件运行。如果后续还要单独运行这些历史脚本，应先把其中的路径更新为新目录。

## 8. 旧目录删除结论

待删除目录：

`C:\Users\Kim81\Documents\workspace\Marketing\舆论\舆论监控2026.7.2`

检查结果：

- 目录存在。
- 文件数：0。
- 子目录数：0。
- Chrome 当前加载路径位于新目录。
- Native Messaging 注册表指向新目录。
- Bridge 进程从新目录启动。
- 活动数据库、导出目录和 Excel 配置均指向新目录。
- 活动数据库中的旧目录路径引用为 0。

**结论：该旧目录现在可以删除，删除不会影响当前插件和 Bridge 运行。**

## 9. 验收标准

1. `/api/health` 返回 `ok=true`。
2. Chrome 扩展实际路径为新目录的 `extension`。
3. Native Host 清单及注册表均指向新目录。
4. 拉取帖子后 Excel 和 SQLite 成功写入。
5. 新用户无 Excel 时能自动创建工作簿。
6. AI 测试连接成功时显示提示。
7. AI 完成后进度到达 100%，结果写回 Excel并显示完成通知。
8. 删除旧目录后 Bridge、侧边栏和已有数据仍可正常访问。

## 10. 实施对齐状态

核验日期：2026-08-11

| 需求 | 状态 | 实现位置 / 核验结果 |
|---|---|---|
| FR-01 页面识别 | 已对齐 | `extension/content.js`：搜索页、首页、推荐流、详情页及新增 DOM 监听 |
| FR-02 私有相关性词库 | 已对齐 | `bridge/data/relevance_keywords.json`（本机忽略）与 `GET /api/relevance` |
| FR-03 帖子与评论拉取 | 已对齐 | `extension/content.js`、`extension/comment-utils.js`：可见 DOM、主/子评论、稳定 ID、部分采集 |
| FR-04 本地写入 | 已对齐 | `bridge/server.py`：SQLite / Excel 幂等写入、首次自动建表、`posts_materials` |
| FR-05 AI 分析 | 已对齐 | `bridge/ai_support.py`、AI 持久化队列、结果回写、失败重试 |
| FR-06 进度反馈 | 已对齐 | Process 五阶段、AI 估算百分比、底部完成通知 |
| FR-07 界面形态 | 已对齐 | Chrome 侧边栏、右侧圆形悬浮球、上下拖动、位置记忆、AI 脉冲状态 |
| FR-08 安装与连接 | 已对齐 | `setup.cmd`、`setup.ps1`、`重新注册插件ID.ps1`、构建/注册/健康检查 |
| 安全与隐私 | 已对齐 | 无 `cookies` / `webRequest` 权限；运行数据、密钥、词库、构建产物均由 `.gitignore` 排除 |
| 运行路径迁移 | 已对齐 | Bridge、注册表、SQLite、Excel 均指向 `舆论监控插件联动`；健康检查版本 `0.18.0` |

### 10.1 本次实机核验

- `GET http://127.0.0.1:17881/api/health` 返回 `ok=true`、`version=0.18.0`。
- `GET /api/relevance` 正常返回本机私有词库，当前为品牌词 7 个、产品词 96 个、账号词 3 个。
- Native Messaging 注册表指向新目录中的 `bridge/com.xhsmonitor.bridge.json`。
- `bridge/native_host_config.json` 中 SQLite、导出目录和两个 CSV 总表均指向新项目目录。
- Git 跟踪文件中未发现个人绝对路径、私有品牌词、访问 Token 或运行时数据。

### 10.2 保留限制

评论完整度、图片 CDN 可用性、透明悬浮球外围、AI 估算百分比及历史 AI 失败任务，继续按第 7 节已知限制处理，不作为本次代码缺陷。
## v0.16.0：拉取状态与 AI 相关性判定

- 打开帖子后，详情卡与贴边 Process 同时显示“已拉取/未拉取”和“相关/不相关/未知”。
- 所有卡片保留拉取按钮；相关性未知时可点击“AI 判断”，模型综合正文、话题与评论判断。
- AI 判定不相关后写入 SQLite，并幂等写入 Excel `sheet3_不相关帖子`，以后扫描直接回显“不相关”。
## v0.16.1：自动注入与断线自愈

- 插件打开、扫描或拉取时若检测到内容脚本未连接，会自动向当前小红书标签页注入全部脚本与样式并重试原操作。
- 无需为扩展重新加载、浏览器休眠或页面历史导航手动刷新小红书页面。
## v0.16.2：ORIGANI Radar 视觉系统

- 重构侧边栏与磁吸 Process：去除网格纸背景，改为精密护肤仪器风格。
- 统一颜色、层级、状态胶囊、队列卡片与键盘焦点；加入随进度填充的信号轨。
- 使用 ImageGen 生成高保真参考稿，并按“打开—观察—操作—再观察”循环实施。

## v0.18.0：Gathered Scenes UI、数据完整性与联动删除

- 侧边栏和详情 Process 使用 Gathered Scenes 证据流 UI：真实帖子首图是事实锚点，朱砂色只标记主动作，纤维缝连接网页证据与本地数据。
- 拉取写入前执行帖子与评论幂等查重；历史评论缺少 ID/时间时，以帖子、作者和原文兜底。
- 无标题帖子以正文前几段派生稳定标题并用于 Excel、SQLite、素材目录与界面；标题修正时按帖子 ID 复用或迁移旧素材目录。
- 评论提取限定在当前详情容器，防止混入搜索页背景 DOM。
- 已拉取帖子可在侧边栏二次确认后联动删除 Excel 帖子整行、全部评论行、SQLite 关联记录与素材目录；逻辑删除提交后若素材清理失败，保留隐藏墓碑目录并返回警告，避免 Excel / SQLite 状态分裂。
