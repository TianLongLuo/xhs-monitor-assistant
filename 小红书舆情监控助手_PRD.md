# 小红书舆情监控助手 PRD

产品版本：`v0.14.0`
PRD 版本：`v2.1`

## 产品目标

通过 Chrome 扩展读取当前页面可见 DOM，将指定品牌与产品相关的帖子、评论和素材写入本地 Excel，并提供可选的 AI 情绪分析与人工复核流程。

## 核心能力

1. 当前页面帖子识别、详情读取与评论采集。
2. Excel / SQLite 幂等写入及素材目录管理。
3. 私有品牌、产品、账号词库配置，不将用户词库提交到代码仓库。
4. AI 队列、进度百分比、完成通知和结果回写。
5. 侧边栏、悬浮窗与右侧吸附悬浮球。
6. 新用户首次拉取自动生成标准 Excel。
7. 一键安装向导完成扩展 ID 与 Native Messaging Bridge 注册。

## 隐私边界

- 扩展不申请 Cookie 权限，不读取 Cookie，不调用小红书隐藏接口。
- Bridge 仅监听本机回环地址。
- 用户数据、AI 配置、数据库和私有词库均由 `.gitignore` 排除。

## 验收标准

- `setup.cmd` 可完成构建、注册和健康检查。
- 拉取后正文、评论、素材可写入本地数据目录。
- AI 任务显示进度并在完成时通知。
- 新用户无 Excel 时自动创建标准工作簿。
- 公开仓库扫描不包含品牌私有词、个人绝对路径、Token 或运行数据。
